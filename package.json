{
  "name": "root",
  "private": true,
  "scripts": {
    "format": "prettier --write packages/**/*.js",
    "lint": "eslint --ignore-path .gitignore --fix packages/"
  },
  "devDependencies": {
    "babel-eslint": "^10.0.3",
    "eslint": "^6.7.2",
    "lerna": "^3.16.4",
    "prettier": "^1.18.2"
  }
}
